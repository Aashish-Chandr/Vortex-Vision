# Lightweight requirements for CI — no GPU deps, no heavy ML frameworks.
# Tests mock all heavy dependencies (torch, ultralytics, confluent-kafka).

# Web Framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
websockets>=12.0

# Database
sqlalchemy[asyncio]>=2.0.29
aiosqlite>=0.20.0
alembic>=1.13.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Monitoring / metrics
prometheus-client>=0.20.0
python-json-logger>=2.0.7

# Data
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0

# Redis (rate limiter)
redis>=5.0.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0
pytest-mock>=3.14.0

# Linting
ruff>=0.4.0
black>=24.4.0
mypy>=1.10.0
