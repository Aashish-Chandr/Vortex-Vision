# CI-only requirements — lightweight, no GPU/ML frameworks.
# Heavy deps (torch, ultralytics, confluent-kafka, ray) are mocked in tests.

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
websockets==12.0

# Database
sqlalchemy[asyncio]==2.0.29
aiosqlite==0.20.0
alembic==1.13.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Metrics
prometheus-client==0.20.0
python-json-logger==2.0.7

# Data (needed by anomaly/transformer tests)
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2

# Redis
redis==5.0.4

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0
pytest-mock==3.14.0
anyio==4.4.0
